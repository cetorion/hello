{
  "name": "hello",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "serve": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "reset": "rm -rf node_modules yarn.lock && yarn cache clean && yarn install",
    "bump": "yarn cache clean && yarn upgrade -L"
  },
  "dependencies": {
    "react": "19.2.1",
    "react-dom": "19.2.1",
    "react-router-dom": "7.10.1"
  },
  "devDependencies": {
    "@types/react": "19.2.7",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "5.1.1",
    "vite": "7.2.6"
  }
}
